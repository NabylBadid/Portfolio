{% extends 'base.html.twig' %}

{% form_theme formProject 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <section class="page-section">
        <div class="container">
            <h1> Création d'un projet </h1>

            {{ form_start(formProject) }}
                {{ form_row(formProject.title) }}
                {{ form_row(formProject.description) }}
                {{ form_row(formProject.image) }}
                {{ form_row(formProject.url) }}
            {{ form_errors(formProject) }}

            <button type="submit" class="btn formButton">Ajouter le projet</button>

            {{ form_end(formProject) }}
        </div>
    </section>
{% endblock %}

